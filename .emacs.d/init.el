
;; Test if config is loaded
(message "Emacs config loaded from ~/dev/dotfiles/.emacs.d!")

;; Disable startup splash screen
(setq inhibit-startup-message t)

;; Enable line numbers globally
(global-display-line-numbers-mode t)

